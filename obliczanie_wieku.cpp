#include <iostream>
#include <vector>
#include "struktura_student.h"

using namespace std;

int obliczanie_wieku (vector<student> studenci, studenci[i].pesel, int i)
{
    int wiek=2023-int(studenci[i].pesel[0])*10-int(studenci[i].pesel[1]);
}